# =============================================================================
# ServiceNow Ticket Analysis Agent - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your credentials

# =============================================================================
# Option 1: Organization's GPT Wrapper API (OAuth2)
# =============================================================================
# Use this if your organization provides a wrapper API for GPT access

# OAuth2 Authentication
AUTH_URL=https://your-org-auth.example.com/oauth2/token
CLIENT_ID=your_client_id_here
CLIENT_SECRET=your_client_secret_here

# GPT Wrapper API
# Format: The full URL will be constructed as: {base_url}/{model}/chat/completions?api-version={version}
GPT_WRAPPER_BASE_URL=https://your-org-gpt-wrapper.example.com
GPT_API_VERSION=2024-02-15-preview

# =============================================================================
# Option 2: Direct OpenAI API
# =============================================================================
# Use this if you have direct access to OpenAI's API
# Note: If wrapper credentials above are provided, they take precedence

OPENAI_API_KEY=sk-your-api-key-here

# =============================================================================
# Model Configuration (Optional)
# =============================================================================
# MODEL_NAME=gpt-4o
